  
  /* Interface is used like a deal, you must get data the same typed and if isn't working correctly is because not has this typed*/
  
  export interface ElementsList {
    _id: number;
    photo: string;
    text: string;
}